# WorldView — Projektregeln

## Git-Workflow

- **Immer direkt auf `main` committen und pushen** — keine neuen Branches erstellen.
- `git push -u origin main`
- Kein Branch-Erstellen, kein PR, außer explizit angewiesen.
